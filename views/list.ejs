<%- include("header") -%>

    <div class="box heading">
        <h1><%=listTitle%></h1>
    </div>

    <div class="box">
        <%tasksList.forEach(task => { %> 
            <form action="/delete" method="POST">
                <div class="item">
                    <input type="checkbox" name="check" value="<%=task._id%>" onchange="this.form.submit()">
                    <p> <%= task.name %> </p>
                </div>
            </form>
        <%}) %>  

        <form class="item" action="/" method="POST">
            <input type="text" id="newTask" name="newTask" required minlength="3" maxlength="30" placeholder="nouvelle tâche" autocomplete="off">
            <button type="submit" id="submit" name="submit">+</button>
        </form>    
    </div>    

<%- include("footer") -%>
